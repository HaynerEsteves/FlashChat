# Uncomment the next line to define a global platform for your project
# this is the podfile for cocoapods. Cocoa was instaled on the mac then on the project. To install cocoapods on the project: Open Terminal > open project folder > run command install cocoapod. The PodFile will be created alongside a new white icon project.
# Open the Podfile with Xcode, and add to it the pods you want, then go to the terminal again and run install pod. Now the pods you want will be installed on the project
platform :ios, '13.0'

target 'Flash Chat iOS13' do
  use_frameworks!

  # Pods for Flash Chat iOS13
  pod 'CLTypingLabel', '~> 0.4.0'
  pod 'FirebaseAuth'
  pod 'FirebaseFirestore'

end
